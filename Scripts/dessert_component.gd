extends Resource
class_name dessert_component

@export var name: String
@export var sprite: Texture2D
#@export var component_scene: PackedScene 
#will this be better if the scenes dont inherit from the resource, as having it cyclical may be an issue?
